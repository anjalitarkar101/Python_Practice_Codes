# Create a lambda function to compute the cube of a number
# Lambda function is used if you need to call a function only once

cube = lambda x : x ** 3        # x is a parameter

# Test the lambda function
print(cube(3))                  # 3 is an argument

